apiVersion: v1
kind: Service
metadata:
  name: trading-ops-console-service
  namespace: default
  labels:
    app: trading-ops-console
spec:
  type: ClusterIP
  selector:
    app: trading-ops-console
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
