apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading-ops-console
  namespace: default
  labels:
    app: trading-ops-console
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trading-ops-console
  template:
    metadata:
      labels:
        app: trading-ops-console
    spec:
      imagePullSecrets:
        - name: ghcr-pull
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: trading-ops-console
          image: ghcr.io/blissmen/trading-ops-console@sha256:6d2ca80cdc5bf4d08bb28970a7f048de70ffbc379e06a1b9c43d576531b9a589
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            failureThreshold: 18
            periodSeconds: 5
            timeoutSeconds: 1
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
              ephemeral-storage: 1Gi
            limits:
              cpu: 500m
              memory: 512Mi
              ephemeral-storage: 1Gi
          securityContext:
            capabilities:
              drop:
                - NET_RAW
